# 推送目标 API（本地运行前端时默认 localhost:3000）
API_URL=http://localhost:3000/api/import-cost-json

# 抓取日期（默认昨天，格式 YYYY-MM-DD）
# FETCH_DATE=2026-05-01

# 无头模式（定时任务必须设为 true）
HEADLESS=true

# Chrome 路径（一般不用改，脚本会自动检测）
# CHROME_PATH=C:\Program Files\Google\Chrome\Application\chrome.exe
